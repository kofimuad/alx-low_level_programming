#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of program
 * @argc: argument count
 * @argv: argument vector
 * Return: (0)
 */

int main(__atrribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
